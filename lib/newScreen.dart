import 'package:flutter/material.dart';
class newScreeen extends StatelessWidget {
  const newScreeen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body:Column(children: [
        Card(color: Colors.redAccent,
        elevation: 5,
        )
      ],),
    );
  }
}
